## Build Spring Boot Apache Ignite Server Node

Build spring boot application:

``` bash
mvn clean package
```

Build spring boot application into a docker image:

``` bash
mvn clean install -Pdocker
```
